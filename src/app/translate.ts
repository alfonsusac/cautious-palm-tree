export function translateX(x: number) {
  if(!x) return ``
  return `translateX(${x}px) `
}
export function translateY(x: number) {
  if(!x) return ``
  return `translateY(${x}px) `
}
export function translateZ(x: number) {
  if(!x) return ``
  return `translateZ(${x}px) `
}